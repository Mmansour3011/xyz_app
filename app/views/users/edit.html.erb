<p style="color: green"><%= notice %></p>
<div id= "heading" class="text-center py-4">
    <h1 class="page-title">Edit profile</h1>
  </div>
  
  <div class="mx-4 md:mx-0">
  <%= render "form", user: @user %>
  <div class=" flex justify-center my-4">
  <%= button_to "Delete user", archiveMe_user_path(@user), method: :patch,data: { confirm: "Are you sure you want to delete this user?" }, class: "form-submit bg-red-600" %>
  </div>
  <div class=" flex justify-center my-4">
    <%= link_to "Back to todos", todos_path,class: "form-submit bg-gray-400"%>
  </div>
  
  </div>